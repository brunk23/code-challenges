#include "compiler.h"
#include "compile_io.h"

/* Use Empty Place Holder for testing purposes */
int process_source(char *filename, int core[]) {
  return 0;
}

/* Use Empty Place Holder for testing purposes */
int output_core(char *filename, int core[]) {
  return 0;
}
